# Copy to /opt/forecast/deploy/ubuntu/forecast.env (systemd EnvironmentFile).
# Основные секреты — в /opt/forecast/.env (см. .env.example в корне репо).
# Цель ≥30 сделок/мес: 15m trend (см. configs/config.yaml → trend_scan).

FORECAST_CONFIG=/opt/forecast/configs/config.yaml
FORECAST_TIMEFRAME=15m
FORECAST_BARS=1000
FORECAST_TOP=20
FORECAST_STAGE1_MIN_SCORE=12
FORECAST_USE_FILTERED=1
FORECAST_USE_CLOSED_BAR=1
FORECAST_LONG_ONLY=0
FORECAST_ALLOW_TREND=1
FORECAST_ALLOW_RANGE=0
FORECAST_MIN_PROB_PCT=0
TREND_LOOKBACK=60
TREND_MIN_MOVE_PCT=0.008
TREND_MIN_REL_VOLUME=1.0
TREND_MIN_REL_VOLUME_RANGE=0.8
TREND_MIN_ATR_PCT=0.003
TREND_TP_TARGET_PCT=0.04
AUTO_TRADE_MIN_SCORE=12
AUTO_TRADE_MIN_RR=1.2
AUTO_TRADE_MIN_PROB_PCT=0

# Auto-trade overrides (ключи AUTO_TRADE_* — в /opt/forecast/.env)
# AUTO_TRADE_ENABLED=true
# AUTO_TRADE_DRY_RUN=true
# AUTO_TRADE_MARKET=spot
